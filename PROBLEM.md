1. ”知识图谱整理“能力出错：[GIN] 2026/06/16 - 17:07:54 | 500 |          2m0s |       127.0.0.1 | POST     "/api/ai/knowledge-graph"
2. ”AI 训练“界面需要优化，并且与对应的功能进行关联
3. “知识图谱” 界面图谱的层级展示过于扁平，优化可视化展现形式
4. “AI分析” 前端表现形式优化，现在是只将json字符串贴在前端了
5. “生成题解”按钮应该在“新建题解”页面提供
6. “做题记录”功能依旧不正常，没有显示绿点
7. 现在存在两个“知识图谱”，一个是静态的学习知识图谱，一个是AI整理的个人知识图谱，没有关联起来


“学习计划”界面让用户能够自己根据题目列表设置自己的题单，就是添加一个“创建题单”按钮，然后让OJ后端提供“按知识点检索题目”和“题单创建”的功能给agent服务，在”知识图谱“这里提供两种功能，一种就是按照做题记录，让AI梳理自己的知识图谱，另一种就是让AI根据用户个人情况再去创建对应的题单。

题单创建的流程：
用户发起请求 -> oj后端 -> agent服务 -(拼接prompt)-> ai 分析 -(json数组的形式 哪些知识点需要补充学习)-> 调用知识点查询题目列表（可以增加筛选条件 比如：知识点、用户没做过的题） -> 得到题目列表 -> agent服务-(拼接prompt)-> ai创建题单 -> 调用题单创建功能

将“AI训练”侧边改成历史记录，将当前的侧边内容直接都去除，然后历史记录支持删除。
====================
当前RAG对文档是怎么切分的？按照结构切分，还是按照语意切分？

检索策略：需要要改成既按题目标签，也要包含用户的提问，注意去重，去掉RAG上下文字符数限制。

“题目题解”生成我觉得需要给AI一个生成的模板：
比如：
    一、问题分析
    二、推理流程
    三、算法设计与分析
    四、代码实现
    五、总结

我觉得需要为当前的AI服务的内容进行重构，因为当前的AI进行方法调用，是根据我们自己编排的流程进行的，并不是AI主动的。go的langchain支持自定义定义AI调用的工具吗？定义后怎么让ai进行使用？而不是采用编排流程的方式。先与我进行探讨。

为“AI训练”界面的AI对话的输入框的下面加一个“+”号，用来让用户选择关联到某道题目，用户可以点击“+”号，然后输入题号，然后就能添加对应的题目信息到上下文中，然后显示题目标签，当然这个标签也可以被用户删除，就像是网页AI对话上传的图片一样，最多关联三道，关联了题目后传输的提示词中会关联上相关的题目信息。先跟我进行探讨。

查看相关readme文档，进行修改优化。另外对fused下的readme的添加项目初始配置说明，详细一点。